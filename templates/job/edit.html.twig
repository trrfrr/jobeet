{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">

        <div class="card p-5 mt-5 shadow p-3 mb-5 bg-white rounded border-top  border-primary">
            <h1>Job edit</h1>
            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_widget(form) }}

            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10 ">
                    <button type="submit" class="btn btn-success">Edit</button>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}